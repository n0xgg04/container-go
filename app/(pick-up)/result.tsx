import * as React from 'react';
import { SafeAreaView } from 'moti';
import { Typography } from '@/shared/components/base';

export default function Result() {
  return (
    <SafeAreaView>
      <Typography color="black">SUCCESS</Typography>
    </SafeAreaView>
  );
}
